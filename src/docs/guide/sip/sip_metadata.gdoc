Metadata concerns the files that are submitted for ingest; but also the operations needed to determine how these
files are processed at ingest time in order to assemble the desired archival package.

When possible, the ingest procedure will add new content metadata about the stored media. For example image resolution,
frames per second for movies.
